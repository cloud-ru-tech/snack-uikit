// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore
fixture('Smoke Test');

// eslint-disable-next-line testcafe-community/missing-expect
test('Default test to prevent testcafe from failing when no tests are found', async () => {});
