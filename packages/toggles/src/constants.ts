export enum Mode {
  Checkbox = 'checkbox',
  Radio = 'radio',
}

export enum Size {
  S = 's',
  M = 'm',
}

export enum FavoriteIcon {
  Star = 'star',
  Heart = 'heart',
}

export enum SelectionMode {
  Single = 'single',
  Multiple = 'multiple',
}
