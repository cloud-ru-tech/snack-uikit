export enum Mode {
  Checkbox = 'checkbox',
  Radio = 'radio',
}

export enum LabelPosition {
  Left = 'left',
  Right = 'right',
}

export enum Width {
  Auto = 'auto',
  Full = 'full',
}

export enum Size {
  S = 's',
  M = 'm',
}

export enum FavoriteIcon {
  Star = 'star',
  Heart = 'heart',
}
