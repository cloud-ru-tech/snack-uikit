export const SEPARATOR = '›';

export enum ItemRenderMode {
  Full = 'full',
  ShortLabel = 'shortLabel',
  Ellipsis = 'ellipsis',
  Collapsed = 'collapsed',
}

export enum ElementType {
  Separator = 'separator',
  Collapse = 'collapse',
  Item = 'item',
}

export enum Size {
  Xs = 'xs',
  S = 's',
}
