export enum StepState {
  Completed = 'completed',
  Current = 'current',
  Loading = 'loading',
  Waiting = 'waiting',
  Rejected = 'rejected',
}

export enum StepValidationResult {
  Completed = 'completed',
  Rejected = 'rejected',
}
