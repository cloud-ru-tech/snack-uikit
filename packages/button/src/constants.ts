export enum Appearance {
  Primary = 'primary',
  Neutral = 'neutral',
  Destructive = 'destructive',
}

export enum HtmlType {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset',
}

export enum Target {
  Blank = '_blank',
  Self = '_self',
  Parent = '_parent',
  Top = '_top',
}

export enum SizeXsM {
  Xs = 'xs',
  S = 's',
  M = 'm',
}

export enum SizeSL {
  S = 's',
  M = 'm',
  L = 'l',
}

export enum IconPosition {
  Before = 'before',
  After = 'after',
}
