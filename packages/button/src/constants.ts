export enum Appearance {
  Primary = 'primary',
  Neutral = 'neutral',
  Red = 'red',
}

export enum HtmlType {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset',
}

export enum Target {
  Blank = '_blank',
  Self = '_self',
  Parent = '_parent',
  Top = '_top',
}

export enum Size {
  S = 's',
  M = 'm',
  L = 'l',
}

export enum IconPosition {
  Before = 'before',
  After = 'after',
}
