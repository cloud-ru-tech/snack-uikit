export enum Type {
  Primary = 'primary',
  Neutral = 'neutral',
  Critical = 'critical',
}

export enum HtmlType {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset',
}

export enum Target {
  Blank = '_blank',
  Self = '_self',
  Parent = '_parent',
  Top = '_top',
}

export enum Size {
  SizeS = 'size-s',
  SizeM = 'size-m',
  SizeL = 'size-l',
}

export enum IconPosition {
  Before = 'before',
  After = 'after',
}
