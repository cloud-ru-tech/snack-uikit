export enum Type {
  Primary = 'primary',
  Neutral = 'neutral',
  Critical = 'critical',
}

export enum Size {
  SizeS = 'size-s',
  SizeM = 'size-m',
  SizeL = 'size-l',
}

export enum Variant {
  LabelOnly = 'label-only',
  IconOnly = 'icon-only',
  LabelIcon = 'label-icon',
}
