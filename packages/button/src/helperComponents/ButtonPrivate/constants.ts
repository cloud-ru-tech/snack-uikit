export enum Variant {
  LabelOnly = 'label-only',
  IconOnly = 'icon-only',
  IconBefore = 'icon-before',
  IconAfter = 'icon-after',
}

export enum PrivateSize {
  Xs = 'xs',
  S = 's',
  M = 'm',
  L = 'l',
}
