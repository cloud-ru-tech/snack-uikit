export enum OnColor {
  InvertNeutral = 'invert-neutral',
  Neutral = 'neutral',
  Primary = 'primary',
  Red = 'red',
  Orange = 'orange',
  Yellow = 'yellow',
  Green = 'green',
  Blue = 'blue',
  Violet = 'violet',
  Pink = 'pink',
}

export enum Size {
  S = 's',
  M = 'm',
  L = 'l',
}

export enum Target {
  Self = '_self',
  Blank = '_blank',
  Parent = '_parent',
  Top = '_top',
}

export enum OnSurface {
  Background = 'background',
  Decor = 'decor',
  Accent = 'accent',
}
